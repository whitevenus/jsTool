export enum Type {
    OBJECT = 'object',
    ARRAY = 'array',
    STRING = 'string',
    UNDEFINED = 'undefined',
    NUMBER = 'number',
    BOOLEAN = 'boolean',
    NULL = 'null',
    SET = 'set'
}
